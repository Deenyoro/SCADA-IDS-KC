# Core dependencies with exact versions
scapy==2.5.0
PyQt6==6.7.0
scikit-learn==1.5.0
joblib==1.5.0
pydantic==2.7.1
PyYAML==6.0.1
requests==2.32.4

# Notifications (cross-platform)
plyer==2.1.0
# win10toast-click==0.1.3  # Windows-only, installed separately during cross-compilation

# Build and packaging
# pyinstaller==6.6.0  # Use latest version instead via build script for ML library bundling fixes

# Testing
pytest==8.2.2
pytest-qt==4.4.0

# Additional dependencies
numpy==1.26.4
pandas==2.2.2
psutil==5.9.8
colorlog==6.8.2
